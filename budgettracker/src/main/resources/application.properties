spring.application.name=budgettracker
spring.datasource.url=jdbc:postgresql://localhost:5432/budget-app
spring.datasource.username=postgres
spring.datasource.password=C0dingR0!
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
```

> Set `ddl-auto=validate` because your tables already exist. This tells JPA to validate the schema without modifying it.

---

## Step 3 ? Create the package structure

Inside `src/main/java/com/budget/budgettracker/`, create these folders (packages):
```
model/
repository/
service/
controller/